后续需要做的工作

  进入 GBBV2 后，先安装依赖：

  # 后端
  cd D:/vscode/GBBV2/server/baby-photo-backend
  npm install
  npx prisma generate

  # 前端
  cd D:/vscode/GBBV2/client/admin-frontend
  npm install
 检查是否有其他未完成的功能或占位页面 
 看看有没有需要修复的 Bug 
 
  准备好开始 Phase 1（基础设施加固）了吗？



 1. Secrets Management: Move live secrets (WeChat
       AppID/Secret) out of .env files, audit all .env files, use
        environment variable injection or a secrets manager.
       2. Security Hardening: Enable HTTPS/SSL, configure proper
       CORS for production, add Content-Security-Policy headers,
       guard Swagger behind NODE_ENV, tighten validation pipe
       mode for production, secure exposed ports (PostgreSQL,
       Redis, Prisma Studio).
       3. CI/CD Pipeline: Create .github/workflows/ with lint,
       test, build, and deploy workflows.
       4. Testing Framework: Create actual test files (unit tests
        for backend, component tests for frontend), set up
       Playwright E2E test configuration.
       5. Docker Infrastructure: Create the missing backend
       Dockerfile (referenced in compose but absent), fix
       lockfile inconsistency (npm vs pnpm), add proper
       healthchecks, separate dev-only services with profiles.
       6. Code Quality: Re-enable TypeScript strict rules in
       ESLint, add pre-commit hooks (husky/lint-staged), fix the
       tsconfig.build.json deleteOutDir issue.
       7. Monitoring & Logging: Add structured logging, add
       health check monitoring endpoint, set up basic application
        metrics.
       8. Configuration Management: Consolidate duplicate
       environment variables (WX_ vs WECHAT_ prefixes), use
       ConfigModule properly, validate env vars at startup.
s


  我已经全面了解了项目。根据探索结果，"基础设施加固"可以涵盖很多
  方面。这是我发现的主要问题领域：                                    
                                                                      
  安全方面：                                                          
  - WeChat 密钥等敏感信息硬编码在 .env 文件中                           - SSL/HTTPS 未配置                                                  
  - Swagger 文档在生产环境暴露                                          - CORS 配置过松                                                     
                                                                      
  基础设施：                                                          
  - 后端 Dockerfile 缺失（docker-compose 引用了但文件不存在）         
  - .github/ 完全为空（无 CI/CD）
  - 无测试文件（Jest/Vitest/Playwright 已配置但无实际测试）
  - Playwright 配置文件缺失

  代码质量：
  - ESLint 中许多 TypeScript 严格规则被禁用
  - 无 pre-commit hooks
  - 前后端构建用 npm 但 Docker 中写的是 pnpm，不一致

  你想优先处理哪些方面？或者直接让我按照优先级排序出一份完整的 Phase 1   计划？
                                 
                                  已完成的工作汇总                                                                                                          
                                                                                                                                 Sprint 1 — 密钥清理                                                                                                          
  - 从 backend/.env 和根 .env 中移除了硬编码的 WeChat AppSecret / JWT_SECRET                                                     - 修正 .gitignore lock-file 段落                          

  Sprint 2 — 后端安全加固
  - CORS: 硬编码数组 → 环境变量可配置
  - ValidationPipe: 生产环境隐藏错误详情、禁止未声明字段
  - Swagger: 仅开发环境开启
  - 监听地址: 生产 127.0.0.1，开发 0.0.0.0
  - 启动检查: 生产环境默认 JWT 密钥时拒绝启动

  Sprint 3 — Docker 修复
  - 创建后端 Dockerfile（多阶段构建 + 非 root 用户）
  - 创建 .dockerignore
  - 所有 Dockerfile pnpm → npm 统一
  - docker-compose healthcheck netstat → wget
  - docker-compose.dev.yml 增加 healthcheck + restart policies

  Sprint 4 — Nginx + 网络安全
  - HTTP 块添加安全头（X-Frame-Options, Referrer-Policy 等）
  - HTTPS 块取消注释（生产可用）
  - PostgreSQL/Redis 端口绑定 127.0.0.1
  - Redis 密码支持、Prisma Studio 加入 db-admin profile

  Sprint 5 — 代码质量
  - deleteOutDir: true
  - noImplicitAny: true
  - ESLint: no-explicit-any → warn, no-floating-promises → error
  - 前端 .prettierrc 新建

  Sprint 6 — 测试基础设施
  - Jest E2E 配置 + 占位测试
  - Vitest 独立配置 + setup
  - Playwright 配置 + 占位 E2E 测试
  - 根 tsconfig.json 清理

  Sprint 7 — 配置清理
  - 移除重复环境变量（保持 WX_APP_ID / WECHAT_APPID 两个必要的）
  - 注释掉 SHADOW_DATABASE_URL
  - 创建 .env.example 模板

  Sprint 8 — Pre-commit Hooks
  - 根 package.json 添加 husky + lint-staged
  - .husky/pre-commit 钩子

  Sprint 9 — CI/CD
  - .github/workflows/ci.yml (lint → test → build)
  - .github/workflows/deploy.yml (Docker 构建推送)

 2026年4月27日的已收款 99，已收款138，未收款-39，是否存在逻辑错误？同时检查申请退款和已退款的业务逻辑。
 1.弹窗的拖拽功能，内容超出屏幕高度时自动出现滚动条，配合全局弹窗拖拽功能，增强操作体验。
