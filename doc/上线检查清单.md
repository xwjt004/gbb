# 上线检查清单

> 基于 Phase 1-4 验收结果，逐项确认可上线

## □ 1. Sentry 错误监控

### 后端
| 项 | 状态 | 操作 |
|----|------|------|
| `@sentry/node` 已安装 | ✅ | package.json 中 |
| `@sentry/profiling-node` 已安装 | ✅ | package.json 中 |
| `Sentry.init()` 已集成 | ✅ | `main.ts:13-19` |
| `Sentry.captureException()` 已集成 | ✅ | `all-exceptions.filter.ts:51-53` |
| **实际接入** | **⬜** | 注册 Sentry → 获取 DSN → 设置 `SENTRY_DSN` 环境变量 |

### 前端
| 项 | 状态 | 操作 |
|----|------|------|
| `@sentry/react` 已安装 | ✅ | package.json |
| `Sentry.init()` 已集成 | ✅ | `main.tsx:11-17` |
| `Sentry.ErrorBoundary` 已包裹 | ✅ | `main.tsx:22-24` |
| **实际接入** | **⬜** | 设置 `VITE_SENTRY_DSN` 环境变量 |

### Sentry 接入步骤
1. 注册 https://sentry.io （免费额度 5 万 events/月）
2. 创建项目 → 选择 Node.js + React
3. 复制 DSN → 设置环境变量：
   ```bash
   # 后端 .env
   SENTRY_DSN=https://xxx@sentry.io/xxx
   SENTRY_TRACES_SAMPLE_RATE=0.1

   # 前端 .env
   VITE_SENTRY_DSN=https://xxx@sentry.io/xxx
   VITE_SENTRY_ENVIRONMENT=production
   ```
4. 验证：手动触发一个 500 错误 → 1 分钟内 Sentry 面板可见

---

## □ 2. 生产环境变量

### 必需配置（必须修改）
| 变量 | 说明 | 检查 |
|------|------|------|
| `DATABASE_URL` | PostgreSQL 连接串 | □ 已配置生产数据库地址/密码 |
| `JWT_SECRET` | JWT 签名密钥（32+ 位随机串） | □ 已改为非默认值 |
| `CORS_ORIGIN` | 前端域名 | □ 已设置为实际域名 |
| `ENCRYPT_KEY` | 数据加密密钥 | □ 已改为非默认值 |

### 微信支付（如启用）
| 变量 | 检查 |
|------|------|
| `WX_APP_ID` / `WX_APP_SECRET` | □ 已配置 |
| `WECHAT_MCH_ID` / `WECHAT_API_KEY` | □ 已配置 |
| `WECHAT_NOTIFY_URL` | □ 已设为生产域名 |

### 安全启动校验
启动时系统会自动检查：
- [x] `JWT_SECRET` 是否为默认值（是则拒绝启动）
- [x] `DATABASE_URL` 是否存在
- [x] 生产环境 `CORS_ORIGIN` 是否配置

---

## □ 3. SSL 证书

### nginx 配置状态
| 项 | 状态 | 说明 |
|----|------|------|
| HTTP → HTTPS 重定向 | ✅ | 已配置 `return 301` |
| ACME 验证路径 | ✅ | `/.well-known/acme-challenge/` 已开放 |
| SSL 证书路径 | ✅ | `/etc/nginx/ssl/cert.pem` + `key.pem` |
| TLS 1.2/1.3 | ✅ | 已配置 |
| HSTS | ✅ | `max-age=31536000; includeSubDomains` |
| CSP | ✅ | `default-src 'self'` |

### 操作步骤
```bash
# 1. 申请证书
sudo bash scripts/setup-ssl.sh your-domain.com admin@your-domain.com

# 2. 修改 nginx.conf 中 server_name 为实际域名

# 3. 重启
docker compose --profile production up -d

# 4. 验证
curl -I https://your-domain.com     # → 200
curl -I http://your-domain.com      # → 301 → https
curl -I https://your-domain.com/api/v1/health  # → 200
```

### 自动续期
每月 1 日凌晨 3 点自动续期，配置在 `setup-ssl.sh` 中已包含。

---

## □ 4. 数据库备份

| 方案 | 说明 | 状态 |
|------|------|------|
| 系统内置定时备份 | SystemBackup 模块，每日备份 | ✅ 已就绪 |
| 宿主机 cron 备份 | pg_dump 到 /backups | ⬜ 需配置 |
| 异地备份 | 上传到 OSS/COS | ⬜ 建议上线后配置 |

生产环境推荐配置宿主机 cron：
```bash
# 每日凌晨 3 点备份
0 3 * * * docker exec gbb-postgres pg_dump -U postgres baby_photo_db | gzip > /backups/baby_photo_$(date +\%Y\%m\%d).sql.gz
# 保留 30 天
0 4 * * * find /backups -name "*.sql.gz" -mtime +30 -delete
```

---

## □ 5. 安全加固

| 项 | 状态 |
|----|------|
| JWT 密钥非默认 | ✅ 启动时强制校验 |
| CORS 限制域名 | ✅ 生产环境需配置 |
| 文件上传类型白名单 | ⬜ 建议上线前完成 |
| CSP 安全头 | ✅ nginx 已配置 |
| XSS 防护头 | ✅ nginx 已配置 |
| 操作日志记录 | ✅ 全局拦截器已集成 |

---

## 签名确认

| 角色 | 签名 | 日期 |
|------|------|------|
| 开发确认 | | |
| 运维确认 | | |
| 业务确认 | | |

---

*基于 Phase 1-4 验收结果生成，最后更新: 2026-05-09*
