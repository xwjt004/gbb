# 乖宝宝儿童影楼管理系统 — 项目当前状态

> **更新日期**：2026-06-08

---

## 当前项目状态总览

| 项目 | 状态 |
|------|------|
| 🏗️ 后端代码 (NestJS) | ✅ 已完成 — 40+ 业务模块 |
| 🎨 前端代码 (React/Admin) | ✅ 已完成 — 60+ 页面，完整路由 |
| 🗄️ 数据库 Schema & Migrations | ✅ 已完成 — 38 个迁移文件已全部部署 |
| 🐳 Docker 编排 | ✅ 已完成 — 生产环境 4 容器运行 |
| 🔐 SSL 证书 | ✅ Let's Encrypt 自动续期 |
| 🌐 域名 DNS | ✅ guaibaobao.cn → A 记录 81.68.236.12 |
| 📝 ICP 备案 | ✅ 辽ICP备2026010503号-1 |
| 🏪 管理后台 | ✅ https://guaibaobao.cn/admin/ |
| 📱 微信小程序 | ✅ 已发布，API 指向生产环境 |
| 💳 微信支付 | ✅ 商户号已配置，API v2/v3 密钥已设置 |
| 🛡️ 安全加固 | ✅ SSH 仅密钥、Fail2Ban、只读容器、安全组 |
| 🔄 自动备份 | ✅ 数据库定时备份 |
| 🐛 Sentry 错误监控 | ✅ 已配置 |
| 📊 Nginx 日志监控 | ✅ 已启用 |

---

## 运行中容器

| 容器名 | 服务 | 状态 | 端口暴露 |
|--------|------|------|---------|
| `gbb-backend` | NestJS API | ✅ Up (healthy) | 仅 Docker 内网 |
| `gbb-postgres` | PostgreSQL 17 | ✅ Up (healthy) | 仅 Docker 内网 |
| `gbb-redis` | Redis 7 | ✅ Up (healthy) | 仅 Docker 内网 |
| `gbb-nginx` | Nginx 反向代理 | ✅ Up | 80/443 |

---

## 部署架构

```
用户请求 → nginx（80/443）
              ├── /admin/*    → 管理后台 SPA（静态文件）
              ├── /api/v1/*   → 反向代理到 gbb-backend:3000
              ├── /uploads/*  → 反向代理到 gbb-backend:3000
              └── /           → 商城首页（预留）
```

---

## 服务器概要

| 项目 | 值 |
|------|-----|
| 云服务商 | 腾讯云轻量应用服务器 |
| IP 地址 | 81.68.236.12 |
| 配置 | 2 核 / 4GB RAM / 80GB SSD |
| 操作系统 | Ubuntu 24.04 |
| Docker 版本 | 29.5.0 |
| 域名 | guaibaobao.cn（阿里云 DNS） |

---

## 后续工作计划

### 短期（1-2 周）

- [ ] 公安联网备案提交
- [ ] 配置商城首页（`client/ps-frontend/` 部署）
- [ ] 代码推送到 GitHub/Gitee 远程仓库（目前仅本地 Git）

### 中期（1-2 个月）

- [ ] 完善 E2E 测试覆盖
- [ ] 配置 CI/CD 流水线
- [ ] 添加业务数据统计报表
- [ ] 小程序功能迭代

### 长期

- [ ] 性能优化（缓存策略、CDN 加速）
- [ ] 数据库读写分离
- [ ] Docker 镜像漏洞扫描（Trivy）
- [ ] PostgreSQL SSL 加密连接

---

## 关键文件清单

| 文件 | 位置 | 用途 |
|------|------|------|
| Docker 编排 | `/opt/gbb/docker-compose.yml` | 定义所有服务 |
| 环境变量 | `/opt/gbb/.env` | 敏感凭据（chmod 600） |
| Nginx 配置 | `/opt/gbb/nginx/nginx.conf` | 反向代理规则 |
| SSL 证书 | `/opt/gbb/nginx/ssl/` | Let's Encrypt（自动续期） |
| 后端 Dist | `/opt/gbb/backend-dist/` | TypeScript 编译产物 |
| 前端静态文件 | `/opt/gbb/www/admin/` | 管理后台 SPA |
| 微信证书 | `/opt/gbb/certs/` | 微信支付 API 证书 |
| 数据库备份 | `/opt/gbb/backups/` | 自动备份文件 |
# 地址
 - 纬度：41.692690  
 - 经度：122.122332

 # 部署
 好的，当前状态总结：                                                                                                                   
                                                                                                                                         
  已完成：                                                                                                                               
  1. npx prisma generate 已在本地运行，生成的 Prisma 客户端包含 latitude/longitude 字段  
  2. 已下载 Linux (Alpine/musl) 版 Prisma 查询引擎二进制 
  3. 已修改生成的 index.js，将 windows 平台目标改为 linux-musl  
  4. 已打包为 /tmp/prisma-client-linux.tar.gz（1.4MB）                                                      
  部署受阻原因：
  - SSH（23.224.187.112:22）连接被拒绝
  - Docker Desktop 无法连接到 Docker Hub（国内网络问题），无法构建镜像

  你需要选择以下任一方式将新 Prisma 客户端部署到服务器：

  方式 A：通过 SSH 手动部署
  # 1. 将 prisma-client-linux.tar.gz 上传到服务器
  scp /tmp/prisma-client-linux.tar.gz root@23.224.187.112:/opt/gbb/

  # 2. SSH 到服务器
  ssh root@23.224.187.112

  # 3. 复制到容器
  docker cp /opt/gbb/prisma-client-linux.tar.gz gbb-backend:/tmp/
  docker exec gbb-backend tar xzf /tmp/prisma-client-linux.tar.gz -C /app/node_modules/.prisma/client/

  # 4. 重启容器
  docker restart gbb-backend

  方式 B：重建 Docker 镜像
  如果 Docker Hub 能连上，运行：
  cd /d/vscode/GBBV2
  docker build -t baby-photo-backend:latest ./server/baby-photo-backend
  docker save baby-photo-backend:latest -o backend-image.tar

  方式 C：通过 GitHub Actions
  将当前修改提交并推送到 main 分支，触发 CI/CD 自动构建和部署。

  另外，关于我们页面的电话号码需要更新为 13841640830（当前是 0416-5577456），需要在退出计划模式后修改。你希望采用哪种部署方式？
# B  选择，的结果
 Docker 镜像构建成功！已保存到 D:\vscode\GBBV2\server\baby-photo-backend\backend-image.tar.gz（401MB）。                                
                                                                                                                                         
  当前无法直接 SSH 到服务器，部署步骤如下：                                                                                                                                                                                                                                       
  在你的机器上执行以下命令：                                                                                                                                                                                                                                                      
  # 1. 上传镜像到服务器
  scp "D:/vscode/GBBV2/server/baby-photo-backend/backend-image.tar.gz" root@23.224.187.112:/opt/gbb/

  # 2. SSH 登录服务器
  ssh root@23.224.187.112

  # 3. 在服务器上加载新镜像
  docker load -i /opt/gbb/backend-image.tar.gz

  # 4. 重启后端容器
  docker compose -f /opt/gbb/docker-compose.prod.yml up -d backend
  # 或
  docker compose -f /opt/gbb/docker-compose.yml --profile production up -d

  # 5. 验证容器启动
  docker logs gbb-backend --tail 20

  这个新镜像包含了更新后的 Prisma schema（含 latitude/longitude 字段），启动后 ShopInfo API 会正常返回坐标数据。
 我的服务器（腾讯云） IP 是 81.68.236.12 

 生产环境是 read_only: true 容器，dist 从 /opt/gbb/backend-dist 挂载。需要同时更新 dist 文件和 Docker 镜像。
 服务器上的 dist 文件已经是最新的。只需要更新 Docker 镜像。直接将镜像流式传输到服务器加载，避免中间文件。
 docker save baby-photo-backend:latest | ssh root@81.68.236.12 "docker load" 2>&1

 docker save baby-photo-backend:latest | ssh root@81.68.236.12 "docker load" 2>&1
   Stream Docker image directly to server


   已更新的文档：                                                                      
                                                                                                                                           1. 1服务器部署指南.md — 后端部署部分修正为实际生产方式（docker build 而非 docker compose build），新增 Prisma Schema 变更时的部署流程
  2. 2乖宝宝儿童影楼服务器部署指南.md — 目录结构修正（移除 prisma-client/，增加                                                            uploads/、certs/），新增容器安全配置说明（read_only、tmpfs、no-new-privileges），更新构建部署流程
  3. 10项目目前状态和下一步工作步骤.md — 补充完整服务器信息表（SSH 用户、项目目录、容器安全配置等）
  4. 记忆已保存 — 我已记录你的腾讯云服务器信息，后续会话可直接引用

  服务器核心信息：
  - IP：81.68.236.12 | SSH 用户：ubuntu
  - 项目目录：/opt/gbb/
  - Docker：29.5.0 | Docker Compose：v5.1.3
  - 后端镜像：baby-photo-backend:latest（本地构建）
  - 安全：read_only 容器 + no-new-privileges + 仅开放 22/80/443 端口
 ssh ubuntu@81.68.236.12

 # 高危警告处理
 ● 已处理完成。操作总结：                            

  已执行： PermitRootLogin yes → PermitRootLogin no                                                                                         
  ┌─────────────────────────────────────┬──────────────────────────────────┐                                                               │               修改前                │              修改后              │
  ├─────────────────────────────────────┼──────────────────────────────────┤
  │ root 可 SSH 登录（yes）             │ root SSH 登录已禁止（no）        │
  ├─────────────────────────────────────┼──────────────────────────────────┤
  │ 腾讯云检测到 root 登录尝试 → 报高危 │ 拒绝所有 root 登录，不再触发告警 │
  └─────────────────────────────────────┴──────────────────────────────────┘

  后续处理：
  1. 这条告警记录登录 https://mc.tencent.com/bS3yCFQ1 → 标记为"已处理/误报"
  2. 以后 root 的登录尝试都会被 SSH 直接拒绝，不会再产生新的高危告警
  3. 日常登录仍用 ssh ubuntu@81.68.236.12 没问题
  # 本地部署命令步骤
    部署步骤很简单，您可以在本地终端执行：                                                                                                 
                                                                                                                                         
  cd D:\vscode\GBBV2\server\baby-photo-backend                                                                                           
                                                                                                                                         
  # 1. 编译                                                                                                                              
  npx tsc                                                                                                                                
                                                                                                                                           # 2. 上传到服务器                                                                                                                      
  scp -r dist/* ubuntu@81.68.236.12:/opt/gbb/backend-dist/                                                                                                                                                                     
  # 3. 重启容器
  ssh ubuntu@81.68.236.12 "sudo docker restart gbb-backend"
# ssh不能使用
终止方案B执行方案A，1. 方案A: 修改配置允许 root 登录（需要设置密码）                                                                   
                                                                                                                                         ● 好的，请在 VNC 依次执行以下命令：                           
                                                                                                                                         
  1. 设置 root 密码                                                                   
                                 
  passwd root                        
  输入新密码并确认（输密码时不会显示字符）。
                                                                                                                                         
  2. 修改 SSH 配置允许 root 登录                                       
                                                                                                                                           sed -i 's/^PermitRootLogin no/PermitRootLogin yes/' /etc/ssh/sshd_config
                                                                                                                                           3. 重启 SSH 服务                                                                    

  systemctl restart ssh

  4. 确认修改生效

  grep PermitRootLogin /etc/ssh/sshd_config
  # 应该输出: PermitRootLogin yes

  完成后告诉我结果，我就可以通过 SSH 连接部署了。
  # 功能升级：一：套系照片缩略图适配优化；二：套系中商品/服务支持名称选择和数量设置 ；三：小程序预约后展示商品/服务缩略图；微信小程序订单中心功能检查增强
  需求一：套系照片缩略图适配优化                                                                                                         
                                                                                                                                         
  问题：后台上传的套系照片有横版和竖版，在小程序预约页面显示时受固定长宽限制，竖版照片不能充分利用屏幕宽度。                             
  目标：竖版照片宽度更接近手机宽度，横版照片正常显示，提升顾客预览体验。                                                                 
                                                                                                                                           需求二：套系中商品/服务支持名称选择和数量设置                                                                                          
                                                                                                                                           问题：新建和编辑套系时，商品内容（如相册、摆台）和服务内容（如服装、化妆）只能简单列出，不能选择具体名称和设置数量。
  目标：支持如"相册×3本、摆台×2个、服装×5套、化妆×3次"等灵活的名称+数量配置。

  需求三：小程序预约后展示商品/服务缩略图

  问题：顾客在小程序点击预约后，在服务内容和商品内容处看不到缩略图。
  目标：在预约确认页面展示商品和服务的缩略图，提升体验。

  需求四：微信小程序订单中心功能检查增强

  问题：订单中心功能需要全面检查和完善（状态显示、按钮逻辑、数据刷新等）。
  目标：确保订单中心各状态显示正确、操作按钮合理、数据及时刷新。

  ---
  这 4 个需求对吗？如果没问题我就开始逐步实施。另外需求 2 涉及到数据库表结构调整（套系项支持名称+数量配置），需要修改 Prisma schema
  和后端 API。

