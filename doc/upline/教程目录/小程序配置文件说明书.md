# 微信小程序管理后台 — 配置项说明

> 对应菜单：**开发管理 → 开发设置**

---

## 一、开发者 ID

| 项目 | 值 | 说明 |
|------|-----|------|
| AppID（小程序ID） | `wxaf0f01abb6462633` | 小程序的唯一标识，项目已有配置 |
| AppSecret（小程序密钥） | 已设置（已加密） | 用于后端调用微信 API，保存在服务器 `.env` 文件中 |

**注意事项：**
- AppSecret 不要泄露给任何人
- 如果不小心泄露，在后台点「重置」生成新的，同时更新服务器 `.env` 文件
- **IP 白名单**未开启即可，开启后反而会限制服务器访问微信 API

---

## 二、服务器域名 ⭐ 已配置 ✅

> **这是小程序开发中最关键的配置，已配置完成。**

**当前状态：**

| 域名类型 | 状态 | 已配域名 |
|---------|------|---------|
| **request 合法域名** | ✅ **已配置** | `https://guaibaobao.cn` |
| **uploadFile 合法域名** | ✅ **已配置** | `https://guaibaobao.cn` |
| **downloadFile 合法域名** | ✅ **已配置** | `https://guaibaobao.cn` |
| socket 合法域名 | ❌ 未配置（不需要） | — |
| WebSocket 合法域名 | ❌ 未配置（不需要） | — |
| udp / tcp 合法域名 | ❌ 未配置（不需要） | — |

**如果不配会怎样：** 小程序里所有 `wx.request()` 请求都会报错 `url not in domain list`，页面空白。

**结论：** ✅ **已配置完成，无需再操作。** 本月还可修改 49 次。

---

## 三、API 安全

**状态：** ✅ 已开启（2026年05月28日 配置完成）

**作用：** 微信对小程序的 API 请求进行加密和签名校验，防止请求被篡改或伪造。这是微信平台自带的安全机制，开启后对开发者透明，不影响正常开发。

**结论：** ✅ 保持开启即可，无需任何操作。

---

## 四、消息推送

**状态：** ❌ 已关闭（当前不需要）

**作用：** 当用户跟小程序有交互时（如提交表单、支付成功等），微信会把事件推送到你的服务器。适用于：
- 用户支付成功后，服务器即时收到通知
- 用户提交客服消息时，服务器自动回复

**我们的项目是否需要：**
- ⬜ **现阶段不需要**
- 🔜 如果以后要做「支付成功回调通知」「客服消息自动回复」，则需要开启并配置服务器地址

**结论：** ❌ 保持关闭，等需要时再配。

---

## 五、扫普通链接二维码打开小程序

**状态：** ❌ 已关闭（当前不需要）

**作用：** 用户用微信扫描一个普通的网址二维码（不是小程序码），可以直接打开小程序的指定页面。

**适用场景：**
- 线下海报上印一个网址二维码，扫码后打开小程序而不是浏览器
- 商品包装上的二维码，扫码直接跳到小程序商品详情

**我们的项目是否需要：**
- ⬜ 如果你有线下宣传物料（海报、名片、展架）需要印二维码，可以用这个功能
- 🔜 以后需要时再配，现在先不管

**结论：** ❌ 保持关闭，等有线下推广需求时再配置。

---

## 六、数据周期性更新

**状态：** ✅ 已开启

**作用：** 小程序每隔 12 小时自动从服务器下载一份 JSON 数据到用户手机本地。即使手机断网，小程序也能读取这份缓存数据正常显示。

**适用场景：**
- 套系列表、商品列表等不频繁变化的数据
- 做离线展示用

**我们的项目是否需要：**
- ✅ 开启也没坏处，但目前项目中没有用到这个能力
- 如果想用，需要开发者在代码里写对应的数据拉取逻辑

**结论：** ✅ 保持开启即可（开启不影响任何功能）。

---

## 七、数据预拉取

**状态：** ✅ 已开启

**作用：** 用户打开小程序之前，微信就在后台提前拉取数据。等用户看到页面时，数据已经 ready，加载速度会更快。

**适用场景：**
- 首页加载速度优化
- 首屏数据预加载

**我们的项目是否需要：**
- ✅ 开启有好处，可以让首页加载稍快一些
- 和上面一样，需要开发者配合写相应逻辑才能生效

**结论：** ✅ 保持开启即可。

---

## 八、安全键盘证书

**状态：** ❌ 已关闭（当前不需要）

**作用：** 用户在输入密码、身份证等敏感信息时，微信提供一个加密键盘，防止输入内容被木马或网络监听窃取。

**适用场景：**
- 金融类小程序（银行、支付）
- 需要输入身份证号、银行卡号等敏感信息的场景

**我们的项目是否需要：**
- ❌ **不需要**。我们是儿童摄影小程序，没有敏感信息输入场景，不用配置。

**结论：** ❌ 保持关闭。

---

## 九、业务域名

**状态：** ❌ 未配置（不需要）

**作用：** 允许在小程序的 `<web-view>` 组件中打开指定域名的网页（相当于在小程序里嵌一个浏览器）。

**适用场景：**
- 在小程序里展示 H5 页面
- 嵌入已有的官网或活动页面

**我们的项目是否需要：**
- ❌ **不需要**。项目中没有任何地方使用 `<web-view>` 组件。

**结论：** ❌ 不用配置。

---

## 十、安全管理配置

**状态：** ❌ 未开启（当前不需要）

**作用：** 对分享请求进行签名校验，防止分享链接被篡改。

**适用场景：**
- 需要分享到微信好友/群时，验证分享来源是否可信

**我们的项目是否需要：**
- ⬜ 以后如果要做分享裂变、分享得积分等活动，可以考虑开启
- 目前分享功能（`onShareAppMessage`）在没有校验的情况下也能正常工作

**结论：** ❌ 保持关闭，以后有分享营销需求时再开启。

---

## 汇总表

| 配置项 | 是否需要 | 当前状态 | 需要操作？ |
|-------|---------|---------|-----------|
| 服务器域名 | ⭐ **必须** | ✅ **已配置** | **无需操作** |
| API 安全 | ✅ 推荐 | ✅ 已开启 | 不动 |
| AppSecret | ✅ 需要 | ✅ 已设置 | 不动 |
| IP 白名单 | ❌ | 未开启 | 不动 |
| 消息推送 | ❌ | 已关闭 | 不动 |
| 扫二维码打开小程序 | ❌ | 已关闭 | 不动 |
| 数据周期性更新 | ⬜ 可选 | 已开启 | 不动 |
| 数据预拉取 | ⬜ 可选 | 已开启 | 不动 |
| 安全键盘证书 | ❌ | 已关闭 | 不动 |
| 业务域名 | ❌ | 未配置 | 不动 |
| 安全管理配置 | ❌ | 未开启 | 不动 |

---

## ✅ 结论

**所有配置已完成，小程序已具备上线条件。** 你不需要再修改任何配置项，直接去上传商品图片、测试功能、提交审核即可。
