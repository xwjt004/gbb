ssh gbb-server 'cat > /opt/baby-photo/server/baby-photo-backend/Dockerfile'─<<─'DOCKERFILE'on-system-implementation ──❯  # ===== Stage 1: base (开发 + 依赖安装) =====                                                                         
   FROM node:20-alpine AS base                                                                                           
   WORKDIR /app                                                                                                          
                                                                                                                         
   # 安装依赖                                                                                                               COPY package.json package-lock.json ./                                                                                
   RUN npm ci                                                                                                                                                                                                                                   
   COPY . .                                                                                                              

   EXPOSE 3000                                                                                                           
                                                                                                                         
   # ===== Stage 2: builder (编译构建) =====                                                                             
   FROM base AS builder
   ENV NODE_OPTIONS="--max-old-space-size=2048"
   RUN npx prisma generate && npm run build

   # ===== Stage 3: runner (生产运行) =====
   FROM node:20-alpine AS runner
   WORKDIR /app

   # 创建非 root 用户
   RUN addgroup -S appgroup && adduser -S appuser -G appgroup

   # 安装健康检查工具
   RUN apk add --no-cache wget

   # 复制编译产物
   COPY --from=builder /app/dist ./dist
   COPY --from=builder /app/node_modules ./node_modules
   COPY --from=builder /app/package.json ./
   COPY --from=builder /app/prisma ./prisma
   COPY docker-entrypoint.sh ./
   RUN chmod +x docker-entrypoint.sh
   COPY --from=builder /app/package.json ./
   COPY --from=builder /app/prisma ./prisma
   COPY docker-entrypoint.sh ./
   RUN chmod +x docker-entrypoint.sh
                             
   # 生成 Prisma Client              
   RUN npx prisma generate
                     
   # 确保非 root 用户可以读写
   RUN chown -R appuser:appgroup /app
              
   # 切换非 root 用户
   USER appuser                                  
             
   EXPOSE 3000              

   ENTRYPOINT ["/bin/sh", "docker-entrypoint.sh"]
   DOCKERFILE
   Upload Dockerfile via SSH
         
 This command requires approval                     
        